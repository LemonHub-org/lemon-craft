[package]
name = "lemoncraft-common-systems"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[features]
simd = ["vek/platform_intrinsics"]

default = ["simd"]

[dependencies]
common = { package = "lemoncraft-common", path = ".." }
common-net = { package = "lemoncraft-common-net", path = "../net" }
common-ecs = { package = "lemoncraft-common-ecs", path = "../ecs" }
common-base = { package = "lemoncraft-common-base", path = "../base" }

rand = { workspace = true }
rayon = { workspace = true }
tracing = { workspace = true }
vek = { workspace = true }
ordered-float = { workspace = true }
itertools = { workspace = true }

# ECS
specs = { workspace = true, features = [
    "serde",
    "storage-event-control",
    "derive",
] }

# Tweak running code
# inline_tweak = { workspace = true, features = ["release_tweak"] }

[dev-dependencies]
# Setup a State
common-state = { package = "lemoncraft-common-state", path = "../state" }
